<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eu.meiremans.winlife.app" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.ReadGoals"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Profile"/>
        <activity android:name=".activities.AddGoal"/>
        <activity android:name=".activities.AddTrophy"/>
        <activity android:name=".activities.Trophy_Details"/>
        <activity android:name=".activities.AndroidDatabaseManager"/>
        <activity android:name=".activities.EditGoal"/>
    </application>

</manifest>
